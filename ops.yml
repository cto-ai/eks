version: "1"
commands:
  - name: eks:1.0.0
    remote: true
    description: An Op to automate the creation and desturction of EKS cluster
    public: false
    sourceCodeURL: ""
    sdk: "2"
    run: npx ts-node /ops/src/index.ts
    src:
      - Dockerfile
      - src
      - package.json
      - .dockerignore
    mountCwd: false
    mountHome: ffalse
    help:
      usage: |-
        This Op requires AWS credentials. Please review the Op readme for details on how to generate and set up these credentials as secrets.

        Options:
          * Create private EKS cluster
          * Destroy private EKS cluster
      arguments: false
      options: false